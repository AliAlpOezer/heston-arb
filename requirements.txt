# Core JAX stack
jax>=0.4.25
jaxlib>=0.4.25
optax>=0.2.0

# Optimization
cvxpy>=1.4.0
scipy>=1.12.0

# Pricing reference
financepy>=0.310
QuantLib>=1.33

# Data
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=15.0.0          # Parquet engine for per-tick chain capture (training dataset)

# Probabilistic (for calibration uncertainty)
numpyro>=0.14.0

# Broker + data feed (Alpaca paper trading / options chain)
alpaca-py>=0.21.0

# Testing
pytest>=8.0.0
pytest-benchmark>=4.0.0
